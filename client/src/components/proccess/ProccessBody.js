import React from 'react'

import Form from "../registration/Form"
import {Conatainer} from "../home-page/TheSystem"

export default function ProccessBody({lang}) {
    return (
        <Conatainer style={{height: "100vh", marginBottom: "0", overflow: "hidden"}}>
            {/* <Form lang={lang}/> */}
        </Conatainer>
    )
}
